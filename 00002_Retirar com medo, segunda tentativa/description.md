Agora prove esta segunda versão que adiciona uma **alternativa condicional**. Não se preocupe com a sintaxe, já que vamos explicar isso. :smile:

``` lenguaje
procedure RetirarAzulComMedo() {
  if (haPedras(Azul)) {
    Retirar(Azul)
  }
}
```
